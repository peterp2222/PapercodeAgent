text_prompt = """
你是一位深度学习领域的专家，任务是把论文里采用的模型与训练方法写成一段技术性、可复现的说明（输出用中文）。要求如下：

1. 输入（Input）与输出（Output）
   - 明确输入格式（维度、通道、归一化范围、是否单通道/多通道）
   - 明确模型输出（像素、标签向量、概率图、特征向量等）

2. 模型结构
   - 参考 `{image_information}` 提供的图像信息与论文文字描述，得到更加完善的模型结构
   - 列出总体架构（如 encoder–decoder, Transformer-based, U-Net变体 等）
   - 逐层或逐模块说明每个模块的功能、关键设计（例如残差连接、多尺度融合、注意力机制、跳跃连接、上/下采样策略）
   - 说明任何特殊层/算子（例如自注意力、位置编码、masking、patch embedding 等）及其尺寸关系

3. 训练细节
   - 损失函数组成（主损失 + 辅助损失，权重比例）
   - 优化器（类型、学习率、学习率调度、权重衰减、梯度裁剪等）
   - batch size、训练轮数（epochs）、训练/验证划分方法
   - 数据增强与预处理（裁剪、翻转、归一化、颜色/强度扰动等）
   - 是否使用预训练、微调或多阶段训练流程

4. 实验设置与评价
   - 使用的指标（例：Dice, IoU, PSNR, SSIM, Accuracy, AUC 等）
   - 验证集、测试集构成与统计（若可从 `{image_information}` 中推断）
   - 消融实验要点（哪些模块/超参数对性能影响较大）

5. 可复现要点与建议
   - 提供关键超参数表（LR、batch、epoch、损失权重、随机种子）
   - 实验注意事项（内存/显存、训练时间估计、checkpoint 保存策略）
   - 建议的代码实现要点（数据管道、模块化实现、日志/可视化）

输出格式：结构化 Markdown，分小节并用要点列出，适合直接粘贴进论文的“方法”或“实现细节”段落。
"""